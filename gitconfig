[core]
	pager = /usr/share/doc/git/contrib/diff-highlight/diff-highlight | less --tabs=4 -RFX
	editor = vim
	quotepath = false
[pager]
	branch = /usr/share/doc/git/contrib/diff-highlight/diff-highlight | less --tabs=4 -RFX
	tag = /usr/share/doc/git/contrib/diff-highlight/diff-highlight | less --tabs=4 -RFX
[user]
	name = Mo Zhou
	email = cdluminate@gmail.com
	editor = vim
[push]
	default = simple
[alias]
	co = checkout
	br = branch
	st = status
	stu = status -uno
	l  = log --stat --show-signature --graph --date=iso
	hist = log --oneline --graph --show-signature
	sub = submodule
	cl = clean -xdf
	w = worktree
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = yellow
	frag = magenta bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[credential]
	helper = cache --timeout=3600
